# frozen_string_literal: true

RSpec.describe Selenium::Webdriver::Element::ExtendClickAgain do
  it 'has a version number' do
    expect(Selenium::Webdriver::Element::ExtendClickAgain::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
